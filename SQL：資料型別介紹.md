| **類別** | **型態名稱**           | **說明**               | **常用情境**                       |
| ------ | ------------------ | -------------------- | ------------------------------ |
| **數值** | `Tiny-INT`         | 微小整數 (範圍 -128 到 127) | 狀態開關 (0 或 1)、性別、年齡             |
|        | Small-INT          |                      |                                |
|        | Medium-INT         |                      |                                |
|        | INT(n)             | 整數(預設寬度n)            |                                |
|        | `BIGINT`           | 超大整數                 | 像 Facebook 這種海量用戶的 ID          |
|        | `DECIMAL`          | 精確的小數 (不會有誤差)        | **金錢、帳單金額**                    |
|        | `FLOAT` / `DOUBLE` | 浮點數 (計算較快但有微小誤差)     | 科學計算、體重、身高                     |
| **字串** | `CHAR(n)`          | 固定長度字串 (不足會補空格)      | 身份證字號、郵遞區號 (長度固定)              |
|        | `VARCHAR(n)`       | 變動長度字串               | 姓名、標題、地址                       |
|        | `TEXT`             | 長篇文字                 | 文章內容、評論                        |
|        | `BLOB`             | 二進位大對象               | 儲存圖片、檔案或加密後的二進位資料              |
| **日期** | `DATE`             | 僅日期 (YYYY-MM-DD)     | 生日、入職日期                        |
|        | `DATETIME`         | 日期 + 時間              | 訂單建立時間 (不受時區影響)                |
|        | `TIMESTAMP`        | 日期 + 時間 (會隨時區切換)     | **系統修改紀錄 (updated_at)**        |
|        | `TIME`             | 僅時間 (HH:MM:SS)       | 營業時間、烹飪耗時                      |
| **特殊** | `ENUM`             | 枚舉 (只能從定義好的清單選一)     | `('small', 'medium', 'large')` |
|        | `JSON`             | 存儲標準 JSON 格式內容       | 靈活的配置項、不固定的屬性清單                |



這張圖片顯示的是 MySQL 資料庫中常見的**數值（Numeric）**資料型態選單。為了讓你清楚如何選擇，我將它們分為「整數」、「浮點數（小數）」及「特殊用途」三大類來介紹：

### 1. 整數型態 (Integers)

這類別用於儲存沒有小數點的全數。選擇的重點在於數字的大小範圍，佔用空間越小，資料庫效能越好。

|資料型態|佔用空間|儲存範圍 (有正負號 Signed)|建議使用場景|
|---|---|---|---|
|**TINYINT**|1 Byte|-128 到 127|開關、狀態值（如：0=停用, 1=啟用）。|
|**SMALLINT**|2 Bytes|-32,768 到 32,767|較小的數量、年份。|
|**MEDIUMINT**|3 Bytes|約 -838 萬 到 838 萬|中等規模的數值。|
|**INT**|4 Bytes|約 **-21 億 到 21 億**|最常用。一般 ID、商品數量、計數器。|
|**BIGINT**|8 Bytes|極大（263−1）|大數據 ID、Facebook/IG 的貼文 ID、毫秒級時間戳。|

---

### 2. 小數與精確數值 (Floating & Fixed Point)

當你需要儲存有小數點的數字（如體重、金額）時使用。

| 資料型態        | 類型  | 特點                            | 建議使用場景        |
| ----------- | --- | ----------------------------- | ------------- |
| **DECIMAL** | 精確值 | **絕不失真**。以字串形式儲存數值。           | **金錢、薪資、稅率**。 |
| **FLOAT**   | 近似值 | 佔 4 Bytes。單精度小數，運算快但有微小誤差。    | 一般科學計算、溫度、體重。 |
| **DOUBLE**  | 近似值 | 佔 8 Bytes。雙精度小數，範圍比 FLOAT 更廣。 | 需要極高範圍的科學數據。  |
| **REAL**    | 近似值 | 在 MySQL 中通常等同於 DOUBLE。        | 跨資料庫系統相容性使用。  |

---

### 3. 特殊數值型態 (Special Types)

選單下方的選項通常用於特定的邏輯或自動化場景。

|**資料型態**|**說明**|
|---|---|
|**BIT**|儲存位元值（0 或 1）。用於二進位資料。|
|**BOOLEAN**|布林值。在 MySQL 中其實是 `TINYINT(1)` 的別名，0 代表 False，1 代表 True。|
|**SERIAL**|**自動遞增的 BigInt**。通常包含 `NOT NULL AUTO_INCREMENT UNIQUE` 屬性，常用於主鍵（PK）。|

---
### MySQL 欄位屬性詳細說明表

|**屬性名稱**|**適用資料類型**|**詳細解釋**|**實際範例**|
|---|---|---|---|
|**BINARY**|字串類 (CHAR, VARCHAR, TEXT)|將字串視為「二進位字串」。主要影響**排序與比較**：預設不分大小寫，勾選後會**區分大小寫**（A ≠ a）。|儲存加密過的二進位原始碼，或強制要求帳號區分大小寫。|
|**UNSIGNED**|數值類 (INT, FLOAT, DECIMAL)|**無符號**。代表該欄位**不允許負數**。因為不儲存負數，正數的可儲存範圍會增加一倍。|`ID` 欄位或`年齡`。例如 `TINYINT` 原本是 -128~127，設定後變成 0~255。|
|**UNSIGNED ZEROFILL**|數值類 (INT)|**自動補零**。繼承了 `UNSIGNED` 的特性，且當數值長度不足時，會在前面自動補 0。|若長度設為 4，數值 `12` 會顯示為 `0012`。常用於編號或月份。|
|**on update CURRENT_TIMESTAMP**|時間類 (TIMESTAMP, DATETIME)|**自動更新時間**。當該列（Row）的任何其他資料被修改時，資料庫會自動將此欄位更新為「當下時間」。|常用於 `updated_at` 欄位，用來追蹤資料最後一次被修改的時間。|
|**COMPRESSED=zlib**|字串或大物件 (BLOB, TEXT)|**資料壓縮**。使用 zlib 演算法在寫入硬碟前對資料進行壓縮，以節省空間，但會稍微增加 CPU 負擔。|儲存非常巨大的文字內容或 Log 日誌，且硬碟空間有限時使用。|
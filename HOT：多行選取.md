在 VS Code 中，這通常被稱為「多行編輯」或「欄位選取」（Column Selection）。這對於需要同時在多行程式碼的同一個位置插入註解、修改縮排或刪除特定字元非常方便。

以下是幾種最實用的操作方式：

### 1. 使用滑鼠滾輪快捷鍵（最直覺）

這是最常用的方法，可以讓你快速垂直拉出一條「閃爍的光標線」。
- **Windows / Linux:** 按住 `Alt` + `Shift` + **滑鼠左鍵拖曳**。
- **macOS:** 按住 `Option` + `Shift` + **滑鼠左鍵拖曳**。
- **操作效果：** 你會看到多行同時出現游標，此時輸入任何字元或按 `Backspace` 刪除，都會在所有選中行同步生效。

---
### 2. 使用鍵盤精確選取

如果你不想用滑鼠，或者需要非常精確的控制，可以使用鍵盤：
- **Windows / Linux:** `Ctrl` + `Alt` + `↑` 或 `↓`
- **macOS:** `Cmd` + `Option` + `↑` 或 `↓`
- **操作效果：** 每按一次方向鍵，游標就會向上或向下增加一個。

---
### 3. 選取一塊「矩形區域」

如果你想選取的不只是游標，而是一個長方形的程式碼塊（例如刪除一整欄的變數類型）：
- **Windows / Linux:** 按住 `Shift` + `Alt` 並用滑鼠選取區域。
- **macOS:** 按住 `Shift` + `Option` 並用滑鼠選取區域。

---

### 實用小技巧對照表

| **功能**      | **Windows / Linux 快捷鍵** | **macOS 快捷鍵**          |
| ----------- | ----------------------- | ---------------------- |
| **向上新增游標**  | `Ctrl` + `Alt` + `↑`    | `Cmd` + `Option` + `↑` |
| **向下新增游標**  | `Ctrl` + `Alt` + `↓`    | `Cmd` + `Option` + `↓` |
| **自由點選多處**  | `Alt` + 滑鼠左鍵點擊          | `Option` + 滑鼠左鍵點擊      |
| **選取所有相同詞** | `Ctrl` + `Shift` + `L`  | `Cmd` + `Shift` + `L`  |

## 自定義程式碼片段(Snippet)

請按照以下步驟操作，只需 1 分鐘：
1. 在 VS Code 中，點擊左下角的 **齒輪圖示**，選擇 **「使用者程式碼片段」(User Snippets)**。
2. 在搜尋框輸入 `php`，選擇 **php (PHP)**。
3. 在開啟的 `php.json` 檔案中，將大括號 `{ }` 內容替換為以下程式碼：
```json
{
  "PHP Tag": {
    "prefix": "php",
    "body": [
      "<?php $0 ?>"
    ],
    "description": "插入 PHP 標籤"
  }
}
```

### 為什麼這樣有效？
- **`prefix`: "php"**：定義你輸入什麼字會觸發。
- **`$0`**：這代表按下 `Tab` 展開後，游標會停在哪個位置（剛好在標籤中間，讓你直接打字）。

### 如何使用？
- 進入 VScode 編輯區，輸入 php + Tab鍵。